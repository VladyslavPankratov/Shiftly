# Production Environment
# Use this as reference for production deployments
# NEVER commit actual production secrets!

DATABASE_URL="<set-via-environment>"
JWT_SECRET="<set-via-environment>"
JWT_EXPIRES_IN="7d"
PORT=3000
NODE_ENV=production
CORS_ORIGIN="https://shiftly.app"
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
