# Staging Environment
# Use this as reference for staging deployments

DATABASE_URL="postgresql://user:password@staging-db.example.com:5432/shiftly_staging?schema=public"
JWT_SECRET="<generate-secure-secret-here>"
JWT_EXPIRES_IN="1d"
PORT=3000
NODE_ENV=staging
CORS_ORIGIN="https://staging.shiftly.app"
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50
